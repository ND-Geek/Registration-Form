import React, {useState} from 'react'



function Props({number,name}) {
    
    return (
        <div>
            <h2>Showcase :  {name+' '+number + 2}</h2>
        </div>
    )
}

export default Props
